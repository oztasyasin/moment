import { StyleSheet } from "react-native";

export const rowStyles = StyleSheet.create({
    row: {
        width: '100%',
        flexDirection: 'row',
        alignItems: 'center',
        maxWidth: 500
    }
})